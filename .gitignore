node_modules/
dist/

# Never commit natural-language secrets. human-to-code also refuses to run
# if this file is git-tracked (see src/discovery.ts).
secrets.human
prompt.txt

.playwright-cli/

issues/

# Written by `npm run stress`; the corpus itself is tracked, its output is not.
test/stress/last-report.json